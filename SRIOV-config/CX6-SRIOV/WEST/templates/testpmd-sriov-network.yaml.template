apiVersion: sriovnetwork.openshift.io/v1
kind: SriovNetwork
metadata:
  name: $VENDOR-$DIR-testpmd-sriov-network
  namespace: openshift-sriov-network-operator
spec:
  ipam: |
    { }
  spoofChk: "on"
  trust: "on"
  resourceName: ${VENDOR}nic_$DIR
  networkNamespace: default
