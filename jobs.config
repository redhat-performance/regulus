#
# per job/run config
#

# Project. Default is crucible-rickshaw
export OCP_PROJECT=crucible-hnhan

# Jobs, a list. This list conforms to make i.e tab in each line, and no quote ""
export JOBS= \
	./1_GROUP/NO-PAO/4IP/INTER-NODE/TCP/2-POD 

# Debug. Default is false
#export DRY_RUN=true

# Artifact directory prefix. Default is null.
export TAG=ovnic-reg-5feb 

# Number of samples per test. Default is 3
export NUM_SAMPLES=1

